{
  "name": "fffmpeg",
  "version": "0.0.1-alpha.1",
  "description": "Another node interface for ffmpeg",
  "main": "lib/index.js",
  "author": "Simon Holmes",
  "license": "MIT",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "bs:setup": "sh bsSetup.sh",
    "prepublish": "npm test && npm run build",
    "test": "nyc node ./test/harness ./test",
    "coveralls": "nyc report --reporter=lcov && cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "coveralls": "^2.11.9",
    "glob": "^7.0.5",
    "nyc": "^6.6.1",
    "tape": "^4.6.0"
  },
  "dependencies": {
    "jest": "^21.2.1"
  }
}
